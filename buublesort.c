#include<stdio.h>
#include<stdlib.h>
void main()
{{
    int n,i,j,temp,scores[30];
    printf("Enter the number of students: ");
    scanf("%d",&n);
    printf("Enter the scores of students: ");
    for(i=0;i<n;i++)
    {
        scanf("%d",&scores[i]);
    }
    

}}